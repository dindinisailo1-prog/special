*{
margin:0;
padding:0;
box-sizing:border-box;
font-family:'Segoe UI',sans-serif;
}

body{
height:100vh;
overflow:hidden;
background:linear-gradient(-45deg,#ffd6e8,#ffeaf4,#ffd9f2,#ffe6fa);
background-size:400% 400%;
animation:bgMove 12s ease infinite;
}

@keyframes bgMove{
0%{background-position:0% 50%}
50%{background-position:100% 50%}
100%{background-position:0% 50%}
}

.screen{
position:absolute;
width:100%;
height:100vh;
display:flex;
flex-direction:column;
justify-content:center;
align-items:center;
text-align:center;
padding:20px;
opacity:0;
pointer-events:none;
transition:opacity .7s ease;
}

.screen.active{
opacity:1;
pointer-events:auto;
}

.title{color:#ff4f9a;margin-bottom:15px;}
.sub{color:#555;margin:5px 0;}

.main-btn{
margin-top:30px;
padding:14px 30px;
border:none;
border-radius:30px;
background:#ff4f9a;
color:white;
font-size:16px;
width:70%;
max-width:250px;
}

/* Girl Animation */
.girl-wrap{
position:relative;
}

.girl{
width:200px;
animation:girlFloat 4s ease-in-out infinite;
}

@keyframes girlFloat{
0%,100%{transform:translateY(0) rotate(0deg)}
50%{transform:translateY(-8px) rotate(-2deg)}
}

/* Couple Animation */
.couple-wrap{
position:relative;
}

.couple{
width:220px;
animation:coupleMove 3s ease-in-out infinite;
}

@keyframes coupleMove{
0%,100%{transform:translateY(0)}
50%{transform:translateY(-6px)}
}

/* Floating Heart between couple */
.mini-heart{
position:absolute;
width:15px;
height:15px;
background:#ff4f9a;
top:40%;
left:50%;
transform:translateX(-50%) rotate(45deg);
animation:heartFloat 2s infinite ease-in-out;
}

.mini-heart:before,
.mini-heart:after{
content:"";
position:absolute;
width:15px;
height:15px;
background:#ff4f9a;
border-radius:50%;
}

.mini-heart:before{top:-7px;left:0;}
.mini-heart:after{left:-7px;top:0;}

@keyframes heartFloat{
0%{opacity:0;transform:translate(-50%,10px) scale(.6) rotate(45deg);}
50%{opacity:1;}
100%{opacity:0;transform:translate(-50%,-20px) scale(1) rotate(45deg);}
}

/* Moving No button */
.btn-group{
position:relative;
width:100%;
height:200px;
}

.yes-btn,.no-btn{
position:absolute;
padding:12px 25px;
border:none;
border-radius:25px;
background:#ff4f9a;
color:white;
}

.yes-btn{left:20%;}
.no-btn{right:20%;}

/* Gift Animation */
.gift-box{
perspective:1000px;
}

.gift-img{
width:180px;
transition:transform 1s ease;
}

.gift-box.open .gift-img{
transform:rotateX(45deg) translateY(-20px);
}

/* Letter */
.letter{
background:white;
padding:25px;
border-radius:20px;
width:85%;
max-width:350px;
box-shadow:0 10px 25px rgba(0,0,0,.1);
}

/* Confetti Canvas */
#confetti{
position:fixed;
top:0;
left:0;
width:100%;
height:100%;
pointer-events:none;
}
